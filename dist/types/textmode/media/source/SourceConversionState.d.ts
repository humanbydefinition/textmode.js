import type { UniformValue } from '../../../rendering/webgl/types/UniformTypes';
import type { SourceGeometryTextureState } from '../../../rendering/webgl/state/TextureState';
import type { RGB, RGBA } from '../../../utils/color';
import { TextmodeColor, type TextmodeColorResolver } from '../../color/TextmodeColor';
import type { TextmodeGlyphAtlas } from '../../fonts/types';
import type { TextmodeSource } from '../TextmodeSource';
import type { GlyphPaletteTextureCache } from './GlyphPaletteTextureCache';
import type { NormalizedConversionStep, SourceColorKind, SourceColorMode } from './types';
export declare class SourceConversionState {
    private readonly _resolveColor;
    private _activeGlyphAtlas;
    private _characterString;
    private _invert;
    private _flipX;
    private _flipY;
    private _charRotation;
    private _brightnessStart;
    private _brightnessEnd;
    private _charColorMode;
    private _cellColorMode;
    private _charColor;
    private _cellColor;
    private _backgroundColor;
    private _glyphColors;
    private _frameInvert;
    private _frameFlipX;
    private _frameFlipY;
    private _frameCharRotation;
    private _frameBrightnessStart;
    private _frameBrightnessEnd;
    private _frameCharColorMode;
    private _frameCellColorMode;
    private _frameCharColor;
    private _frameCellColor;
    private _frameBackgroundColor;
    private _frameGlyphColors;
    constructor(_resolveColor?: TextmodeColorResolver);
    get activeGlyphAtlas(): TextmodeGlyphAtlas | null;
    setActiveGlyphAtlas(font: TextmodeGlyphAtlas, paletteCache: GlyphPaletteTextureCache): boolean;
    setInvert(value: boolean | number, frame: boolean): void;
    setFlipX(value: boolean | number, frame: boolean): void;
    setFlipY(value: boolean | number, frame: boolean): void;
    setCharRotation(degrees: number, frame: boolean): void;
    setBrightnessRange(start: number, end: number, frame: boolean): void;
    setCharColorMode(mode: SourceColorMode, frame: boolean): void;
    setCellColorMode(mode: SourceColorMode, frame: boolean): void;
    setColor(kind: SourceColorKind, frame: boolean, colorOrGray: number | string | TextmodeColor, g?: number, b?: number, a?: number): void;
    setCharacters(chars: string, frame: boolean, paletteCache: GlyphPaletteTextureCache): void;
    clearFrameOverrides(paletteCache: GlyphPaletteTextureCache): void;
    hasFrameUniformOverrides(): boolean;
    getCharacterPalette(chars: string): RGB[];
    createBaseUniforms(texture: WebGLTexture, step: NormalizedConversionStep | null, paletteCache: GlyphPaletteTextureCache): Record<string, UniformValue>;
    createGeometryTextureSnapshot(source: TextmodeSource, paletteCache: GlyphPaletteTextureCache): SourceGeometryTextureState;
    get debugSnapshot(): {
        invert: number;
        flipX: number;
        flipY: number;
        charRotation: number;
        brightnessStart: number;
        brightnessEnd: number;
        charColorMode: SourceColorMode;
        cellColorMode: SourceColorMode;
        charColor: RGBA;
        cellColor: RGBA;
        backgroundColor: RGBA;
        glyphColors: RGB[];
    };
    private _setNumber;
    private _getColorTarget;
    private _applyCharacterPalette;
}
